(defwidget logo []
    (eventbox :onhover "scripts/toggle_notifications_menu.sh &"
        (box :class "logo" :orientation "h" :space-evenly false :halign "start" :valign "center"
            (label :text "󰣇")
        )
    )
)

(defwidget logo_power []
    (eventbox :onhover "scripts/toggle_settings_menu.sh &"
        (box :class "logo-power" :orientation "h" :space-evenly false :halign "start"
            (label :text "󰐥")
        )
    )
)