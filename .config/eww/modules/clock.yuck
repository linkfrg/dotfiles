(defwidget clock []
    (eventbox :onhover "scripts/toggle_control_center.sh open &"
      (box :class "clock" :orientation "v" :space-evenly false
        (label :text {formattime(EWW_TIME, "%H")})
        (label :text {formattime(EWW_TIME, "%M")})
      )
    )
)